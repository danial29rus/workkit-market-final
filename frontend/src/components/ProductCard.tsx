import {Link} from 'react-router-dom'; import {ArrowUpRight,Star} from 'lucide-react'; import type {Product} from '../types'
const money=(v:string)=>new Intl.NumberFormat('ru-RU',{style:'currency',currency:'RUB'}).format(Number(v))
export default function ProductCard({p}:{p:Product}){const v=p.variants[0];return <article className="productCard"><Link to={`/product/${p.slug}`}><div className="imgWrap"><img src={p.image_url} alt=""/><span className="badge">{p.category_name}</span></div><div className="pcBody"><h3>{p.title}</h3><p>{p.short_description}</p><div className="rating"><Star size={15} fill="currentColor"/> 4.9 <span>по отзывам клиентов</span></div><div className="priceRow"><div><small>от</small><strong>{money(v.price)}</strong>{v.old_price&&<del>{money(v.old_price)}</del>}</div><button aria-label="Открыть услугу"><ArrowUpRight size={17}/></button></div></div></Link></article>}
