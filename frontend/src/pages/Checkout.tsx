import{FormEvent,useEffect,useState}from'react';import{Link,useNavigate,useParams}from'react-router-dom';import{api,session}from'../api';import type{Product,User,Variant}from'../types';import{CreditCard,ShieldCheck,UserRound}from'lucide-react';import{useSite}from'../site'
const money=(v:string)=>new Intl.NumberFormat('ru-RU',{style:'currency',currency:'RUB'}).format(Number(v))
export default function Checkout(){const{variantId}=useParams();const nav=useNavigate();const[p,setP]=useState<Product[]>([]);const[user,setUser]=useState<User|null>(session.user());const[busy,setBusy]=useState(false);const site=useSite();useEffect(()=>{api.products().then(setP);if(session.token())api.me().then(x=>{setUser(x);localStorage.setItem('workkit_user',JSON.stringify(x))}).catch(()=>session.logout())},[]);let product:Product|undefined,variant:Variant|undefined;for(const x of p){const v=x.variants.find(v=>v.id===Number(variantId));if(v){product=x;variant=v;break}}async function submit(e:FormEvent){e.preventDefault();if(!variant||!user)return;setBusy(true);try{const order=await api.createOrder(variant.id);localStorage.setItem('workkit_last_order',order.public_id);nav(`/success?order=${order.public_id}`)}finally{setBusy(false)}}if(!product||!variant)return <div className="container page">Загрузка…</div>;return <div className="container page"><div className="pageTitle"><span className="crumb">Главная / Оформление</span><h1>Оформление заказа</h1><p>Сейчас создаётся заказ без списания средств. После подключения агрегатора кнопка оплаты будет вести на защищённую страницу платёжного партнёра.</p></div><div className="checkout"><form className="checkoutForm" onSubmit={submit}><div className="formCard"><h2>Заказчик</h2>{user?<div className="signedCustomer"><UserRound/><div><b>{user.full_name||'Клиент'}</b><span>{user.email}{user.phone?` · ${user.phone}`:''}</span></div><Link to="/account">Кабинет</Link></div>:<div className="loginRequired"><UserRound/><div><b>Нужен аккаунт</b><span>Чтобы заказ сохранился в личном кабинете, войдите или зарегистрируйтесь.</span></div><Link className="primary" to="/login">Войти</Link></div>}</div><div className="formCard disabledPay"><h2>Оплата</h2><div className="payOption"><CreditCard/><div><b>Платёжный агрегатор</b><span>Интеграция будет добавлена следующим этапом</span></div><span className="statusPill">Скоро</span></div><p>Номер карты, срок действия и CVV сайт не запрашивает и не хранит. После выбора агрегатора здесь будет redirect / hosted checkout.</p></div><label className="accept"><input type="checkbox" required/><span>Я принимаю <Link to="/offer">условия публичной оферты</Link> и согласен с обработкой данных согласно <Link to="/privacy">политике конфиденциальности</Link>.</span></label><button className="primary wide" disabled={busy||!user}>{busy?'Создаём заказ…':user?site.order_cta:'Сначала войдите в аккаунт'}</button></form><aside className="summary"><h3>Ваш заказ</h3><div className="summaryProduct"><img src={product.image_url}/><div><b>{product.title}</b><span>Пакет: {variant.name}</span></div></div><div className="line"><span>Стоимость</span><b>{money(variant.price)}</b></div><div className="total"><span>Итого</span><b>{money(variant.price)}</b></div><div className="safe"><ShieldCheck/><span>После создания заказ сразу появится в личном кабинете.</span></div></aside></div></div>}
